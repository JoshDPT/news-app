import { categories } from "../constants";

export default async function HomePage() {
  // fetch the news data 
  // const news = await fetchNews();
  return (
    <div>
      {/* NewsList news */}
    </div>
  )
}